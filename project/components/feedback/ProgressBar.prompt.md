A segmented/linear progress indicator — used for "You're 68% complete" on the Mission Card (never a raw percentage label alone) and session question-progress.

```jsx
<ProgressBar value={68} tone="light" />
```

Use `tone="light"` on the dark Mission Card surface, `tone="blue"` on white cards.
