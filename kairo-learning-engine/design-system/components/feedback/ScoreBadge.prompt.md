The KAIRO Score display (45% Accuracy / 35% Retention / 20% Consistency composite) — always shown with its movement delta, never a bare number with no explanation elsewhere on screen.

```jsx
<ScoreBadge score={842} delta={12} />
```
