The Session Completion screen's core content block — leads with plain-language headline (never a bare percentage), never a raw leaderboard/verdict.

```jsx
<SessionSummaryCard headline="You reinforced 2 concepts and steadied 1 that was fading" strengths={['Cell Division — Reinforced']} nextSteps={['Review: Atomic Structure (still fading)']} scoreDelta={12} />
```
