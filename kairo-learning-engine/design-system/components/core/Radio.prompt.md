A standard radio button — e.g. Smart Practice vs Manual Practice, or Untimed vs Timed session creation choices.

```jsx
<Radio checked={mode === 'smart'} onChange={() => setMode('smart')} label="Smart Practice" />
```
